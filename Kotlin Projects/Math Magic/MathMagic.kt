fun main() {
  // This program is calculating my age through various operations.
  val userName = "Jason Hammar"
  var magicNum: Int
  magicNum = 2 * 5// Multiplies the first digit of my age by 5
  magicNum += 3// Adds 3 to magicNum
  magicNum *= 2// Multiplies 2 to magicNum
  magicNum += 2// Adds the second digit of my age to magicNum
  magicNum -= 6// Subtracts 6 from magicNum
  println(userName + "\'s age is " + magicNum)// Prints out the age.
}
